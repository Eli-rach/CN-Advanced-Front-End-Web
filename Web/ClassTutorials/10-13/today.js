// import URLBuilder from "./URLBuilder";

// const urlBuilder = new URLBuilder("www.google.com")
// console.log(urlBuilder.url)
const month = "October";
let day = 13;

function printDay() {
    console.log(`The Month is: ${month} and the day is: ${day}`);
}

export{ month, day, printDay}